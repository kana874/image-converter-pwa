sc(n)}</span><span class="${v?"cap-ok":"cap-ng"}">${v?"利用可能":"利用不可"}</span></div>`).join("");
 if(els.webpOption)els.webpOption.disabled=!capabilities.webp;
 const limitations=[];if(!capabilities.clipboard)limitations.push("画像のクリップボードコピー");if(!capabilities.webp)limitations.push("WebP出力");if(!capabilities.storage)limitations.push("設定・プリセットの保存");if(!engineReady&&capabilities.heic===false)limitations.push("HEIC / HEIF変換");
 if(limitations.length){els.browserNotice.classList.remove("hidden");els.browserNotice.innerHTML=`<strong>一部機能に制限があります。</strong> ${esc(limitations.join("、"))}。利用可能な機能はそのまま使用できます。`}else els.browserNotice.classList.add("hidden");
}

function syncQualityFromRange(){const q=clampQuality(els.quality.value);els.quality.value=String(q);els.qualityNumber.value=String(q)}
function syncQualityFromNumber(){const q=clampQuality(els.qualityNumber.value);els.qualityNumber.value=String(q);els.quality.value=String(q)}
function updateFormatUi(){const mime=els.outputFormat.value,isPng=mime==="image/png",isJpeg=mime==="image/jpeg";els.quality.disabled=els.qualityNumber.disabled=runtime.running||isPng;els.alphaField.classList.toggle("hidden-field",isJpeg);els.jpegBgField.classList.toggle("hidden-field",!isJpeg);els.alphaColorField.classList.toggle("hidden-field",isJpeg||els.alphaMode.value!=="custom");els.formatNote.textContent=isPng?"PNGは可逆圧縮のため品質値は使用しません。":mime==="image/webp"?"WebPは品質設定を使用します。透明保持にも対応します。":"JPEGは品質設定を使用します。透明部分は指定背景色へ統合します。";els.convertBtn.textContent=getConvertButtonText()}
function updateAdvancedUi(){
 const cr=els.customResizeEnable.checked,ts=els.targetSizeEnable.checked;els.customResizeFields.classList.toggle("disabled",!cr);[els.customWidth,els.customHeight,els.keepAspect,els.noUpscale].forEach(e=>e.disabled=runtime.running||!cr);els.targetSizeFields.classList.toggle("disabled",!ts);[els.targetSizeValue,els.targetSizeUnit,els.sizeResizeFallback].forEach(e=>e.disabled=runtime.running||!ts);
 els.commonNameField.classList.toggle("hidden-field",els.filenamePreset.value!=="common_sequence");els.zipCommonNameField.classList.toggle("hidden-field",els.zipPreset.value!=="common_date");updateFormatUi();updateNamingPreviews();
}

function filenameTemplateForPreset(p){return FILENAME_PRESET_TEMPLATES[p]||els.filenameTemplate.value||"{name}"}
function zipTemplateForPreset(p){return ZIP_PRESET_TEMPLATES[p]||els.zipTemplate.value||"画像変換_{date}"}
function setFilenamePresetTemplate(){if(els.filenamePreset.value!=="custom")els.filenameTemplate.value=filenameTemplateForPreset(els.filenamePreset.value);markCurrentCustom(true);updateAdvancedUi()}
function setZipPresetTemplate(){if(els.zipPreset.value!=="custom")els.zipTemplate.value=zipTemplateForPreset(els.zipPreset.value);markCurrentCustom(true);updateAdvancedUi()}
function insertAtCursor(input,text){const s=input.selectionStart??input.value.length,e=input.selectionEnd??input.value.length;input.value=input.value.slice(0,s)+text+input.value.slice(e);const p=s+text.length;input.focus();input.setSelectionRange?.(p,p);input.dispatchEvent(new Event("input",{bubbles:true}))}
function renderTemplate(template,ctx){let s=String(template||"");for(const [k,v] of Object.entries(ctx))s=s.replace(new RegExp(`\\{${k}\\}`,"g"),()=>String(v??""));return s}
function sequenceValue(index){const start=Math.max(0,Math.floor(Number(els.sequenceStart.value)||0)),digits=clamp(Math.floor(Number(els.sequenceDigits.value)||3),1,8);return String(start+index).padStart(digits,"0")}
function filenameContext(job,index,result=null){const ext=getOutputExt(els.outputFormat.value),out=result||job?.output,w=out?.width??"幅",h=out?.height??"高さ";return{name:safeBaseName(job?.file?.name||"IMG_0001.HEIC"),index:sequenceValue(index),date:dateStamp(),time:timeStamp(),width:w,height:h,resolution:`${w}x${h}`,format:ext,quality:String(clampQuality(els.quality.value)),origext:originalExt(job?.file?.name||"heic"),common:els.commonName.value.trim()||"画像",ext}}
function buildOutputName(job,index,result,used){const ext=getOutputExt(els.outputFormat.value),tpl=els.filenamePreset.value==="custom"?(els.filenameTemplate.value.trim()||"{name}"):filenameTemplateForPreset(els.filenamePreset.value);let raw=renderTemplate(tpl,filenameContext(job,index,result));raw=ensureExt(sanitizeFilename(raw),ext);return uniqueName(raw,used)}
function zipContext(count){const first=jobs[0]?safeBaseName(jobs[0].file.name):"IMG_0001",c=Math.max(0,count);return{date:dateStamp(),time:timeStamp(),count:c,first,rest:Math.max(0,c-1),common:els.zipCommonName.value.trim()||"画像変換"}}
function buildZipName(count){const tpl=els.zipPreset.value==="custom"?(els.zipTemplate.value.trim()||"画像変換_{date}"):zipTemplateForPreset(els.zipPreset.value);return ensureExt(sanitizeFilename(renderTemplate(tpl,zipContext(count))),"zip")}
function updateNamingPreviews(){
 const sampleJobs=jobs.length?jobs.slice(0,3):[{file:{name:"IMG_1884.HEIC"}},{file:{name:"IMG_1885.HEIC"}},{file:{name:"IMG_1886.HEIC"}}],used=new Set(),lines=[],rawNames=[];sampleJobs.forEach((j,i)=>{const ext=getOutputExt(els.outputFormat.value),tpl=els.filenamePreset.value==="custom"?(els.filenameTemplate.value.trim()||"{name}"):filenameTemplateForPreset(els.filenamePreset.value);let raw=ensureExt(sanitizeFilename(renderTemplate(tpl,filenameContext(j,i,j.output))),ext);rawNames.push(raw.toLowerCase());lines.push(uniqueName(raw,used))});const dup=rawNames.length!==new Set(rawNames).size;
 els.filenamePreview.innerHTML=`<div class="preview-title">保存名の例</div>${lines.map(x=>`<div class="preview-line">${esc(x)}</div>`).join("")}${dup?'<div class="preview-warn">重複名は保存時に自動で番号を追加します。</div>':""}`;
 const count=jobs.filter(j=>j.output).length||jobs.length||3;els.zipPreview.innerHTML=`<div class="preview-title">保存予定ZIP</div><div class="preview-line">${esc(buildZipName(count))}</div>`;
}

function collectSettings(){return{outputFormat:els.outputFormat.value,quality:clampQuality(els.quality.value),resizePreset:els.resizePreset.value,customResizeEnable:els.customResizeEnable.checked,customWidth:els.customWidth.value,customHeight:els.customHeight.value,keepAspect:els.keepAspect.checked,noUpscale:els.noUpscale.checked,targetSizeEnable:els.targetSizeEnable.checked,targetSizeValue:Number(els.targetSizeValue.value)||500,targetSizeUnit:els.targetSizeUnit.value,sizeResizeFallback:els.sizeResizeFallback.checked,alphaMode:els.alphaMode.value,alphaColor:els.alphaColor.value,jpegBgColor:els.jpegBgColor.value,removeMetadata:els.removeMetadata.checked,filenamePreset:els.filenamePreset.value,filenameTemplate:els.filenameTemplate.value,commonName:els.commonName.value,sequenceStart:Number(els.sequenceStart.value)||0,sequenceDigits:els.sequenceDigits.value,zipPreset:els.zipPreset.value,zipTemplate:els.zipTemplate.value,zipCommonName:els.zipCommonName.value}}
function applySettings(s,markPreset=false){suppressSettingEvents=true;try{const base={...BUILTIN_PRESETS.standard.settings,...s};const set=(el,v)=>{if(el&&v!==undefined&&v!==null)el.value=String(v)},check=(el,v)=>{if(el&&v!==undefined)el.checked=!!v};set(els.outputFormat,base.outputFormat);if(els.outputFormat.value==="image/webp"&&!capabilities.webp)set(els.outputFormat,"image/jpeg");set(els.quality,base.quality);set(els.qualityNumber,base.quality);set(els.resizePreset,base.resizePreset);check(els.customResizeEnable,base.customResizeEnable);set(els.customWidth,base.customWidth);set(els.customHeight,base.customHeight);check(els.keepAspect,base.keepAspect);check(els.noUpscale,base.noUpscale);check(els.targetSizeEnable,base.targetSizeEnable);set(els.targetSizeValue,base.targetSizeValue);set(els.targetSizeUnit,base.targetSizeUnit);check(els.sizeResizeFallback,base.sizeResizeFallback);set(els.alphaMode,base.alphaMode);set(els.alphaColor,base.alphaColor);set(els.jpegBgColor,base.jpegBgColor);check(els.removeMetadata,base.removeMetadata);set(els.filenamePreset,base.filenamePreset);set(els.filenameTemplate,base.filenameTemplate||filenameTemplateForPreset(base.filenamePreset));set(els.commonName,base.commonName);set(els.sequenceStart,base.sequenceStart);set(els.sequenceDigits,base.sequenceDigits);set(els.zipPreset,base.zipPreset);set(els.zipTemplate,base.zipTemplate||zipTemplateForPreset(base.zipPreset));set(els.zipCommonName,base.zipCommonName);syncQualityFromRange()}finally{suppressSettingEvents=false}updateAdvancedUi();if(!markPreset)markCurrentCustom(false)}
function loadUserPresets(){userPresets=safeJson(lsGet(STORAGE.presets,"[]"),[]);if(!Array.isArray(userPresets))userPresets=[]}
function saveUserPresets(){lsSet(STORAGE.presets,JSON.stringify(userPresets))}
function populatePresetSelect(selected="builtin:standard"){
 const current=selected;els.presetSelect.innerHTML='<option value="custom">カスタム（現在の設定）</option>'+Object.entries(BUILTIN_PRESETS).map(([k,v])=>`<option value="builtin:${esc(k)}">${esc(v.label)}</option>`).join("")+(userPresets.length?'<optgroup label="保存したプリセット">'+userPresets.map(p=>`<option value="user:${esc(p.id)}">${esc(p.name)}</option>`).join("")+'</optgroup>':"");if([...els.presetSelect.options].some(o=>o.value===current))els.presetSelect.value=current;else els.presetSelect.value="custom";updateDeletePresetButton()}
function markCurrentCustom(saveLast=true){if(!suppressSettingEvents){els.presetSelect.value="custom";updateDeletePresetButton();if(saveLast)lsSet(STORAGE.last,JSON.stringify({version:11,settings:collectSettings()}))}}
function updateDeletePresetButton(){els.deletePresetBtn.disabled=!els.presetSelect.value.startsWith("user:")||!capabilities.storage}
function applySelectedPreset(){const v=els.presetSelect.value;if(v.startsWith("builtin:")){const k=v.slice(8);applySettings(BUILTIN_PRESETS[k]?.settings||BUILTIN_PRESETS.standard.settings,true)}else if(v.startsWith("user:")){const id=v.slice(5),p=userPresets.find(x=>x.id===id);if(p)applySettings(p.settings,true)}updateDeletePresetButton();lsSet(STORAGE.last,JSON.stringify({version:11,settings:collectSettings()}))}
function saveCurrentPreset(){if(!capabilities.storage){setStatus("このブラウザではプリセットを保存できませ