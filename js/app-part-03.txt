ん。","warn");return}const name=els.presetName.value.trim();if(!name){setStatus("プリセット名を入力してください。","warn");return}const id=`p${Date.now().toString(36)}`;userPresets.push({id,name,settings:collectSettings()});saveUserPresets();populatePresetSelect(`user:${id}`);els.presetName.value="";setStatus(`プリセット「${name}」を保存しました。`,"success")}
function deleteSelectedPreset(){const v=els.presetSelect.value;if(!v.startsWith("user:"))return;const id=v.slice(5),p=userPresets.find(x=>x.id===id);userPresets=userPresets.filter(x=>x.id!==id);saveUserPresets();populatePresetSelect("builtin:standard");applySettings(BUILTIN_PRESETS.standard.settings,true);setStatus(p?`プリセット「${p.name}」を削除しました。`:"プリセットを削除しました。","success")}

function releaseOutput(job){if(!job.output)return;for(const key of ["url","previewUrl"]){const u=job.output[key];if(u)try{URL.revokeObjectURL(u)}catch(_){}}job.output=null}
function releaseAllOutputs(){jobs.forEach(releaseOutput)}
function clearAll(){if(runtime.running)return;releaseAllOutputs();jobs=[];renderAll();setStatus("")}
function addFiles(incoming){if(runtime.running)return;const all=Array.from(incoming),accepted=all.filter(isLikelyImage),existing=new Set(jobs.map(j=>`${j.file.name}|${j.file.size}|${j.file.lastModified}`));for(const f of accepted){const k=`${f.name}|${f.size}|${f.lastModified}`;if(!existing.has(k)){jobs.push({id:`j${Date.now().toString(36)}${Math.random().toString(36).slice(2,7)}`,file:f,status:"waiting",stage:"",error:null,output:null,rotation:0,original:{name:f.name,size:f.size,type:f.type,width:null,height:null}});existing.add(k)}}renderAll();const rejected=all.length-accepted.length;setStatus(rejected?`${rejected} 個の非対応ファイルを除外しました。`:"",rejected?"warn":"")}
function removeJob(id){if(runtime.running)return;const i=jobs.findIndex(j=>j.id===id);if(i>=0){releaseOutput(jobs[i]);jobs.splice(i,1);renderAll();setStatus("")}}
function normalizeRotation(value){const n=((Math.round(Number(value)||0)%360)+360)%360;return n===90||n===180||n===270?n:0}
function rotationLabel(value){const r=normalizeRotation(value);return r===90?"右90°":r===180?"180°":r===270?"左90°":"変更なし"}
function rotateJob(id,delta){if(runtime.running)return;const job=jobs.find(j=>j.id===id);if(!job)return;releaseOutput(job);job.rotation=normalizeRotation((job.rotation||0)+delta);job.status="waiting";job.stage="";job.error=null;job.copyState="";renderAll();setStatus(`${job.file.name}：向きを「${rotationLabel(job.rotation)}」に設定しました。`,"success")}
function resetJobRotation(id){if(runtime.running)return;const job=jobs.find(j=>j.id===id);if(!job)return;releaseOutput(job);job.rotation=0;job.status="waiting";job.stage="";job.error=null;job.copyState="";renderAll();setStatus(`${job.file.name}：向きを元に戻しました。`,"success")}
function stateLabel(j){return j.status==="processing"?(j.stage||"処理中"):j.status==="done"?"完了":j.status==="warn"?"警告":j.status==="error"?"失敗":j.status==="cancelled"?"中止":"待機"}
function renderFiles(){els.list.innerHTML="";if(!jobs.length){els.list.innerHTML='<div class="empty">画像ファイルを追加してください。</div>';els.summary.textContent="0 ファイル";return}els.summary.textContent=`${jobs.length} ファイル・${fmtBytes(jobs.reduce((s,j)=>s+j.file.size,0))}`;for(const j of jobs){const row=document.createElement("div");row.className="file-row";const ext=(j.file.name.split(".").pop()||"IMG").toUpperCase().slice(0,6),rotation=normalizeRotation(j.rotation);row.innerHTML=`<div class="file-main"><div class="file-icon">${esc(ext)}</div><div class="file-meta"><div class="file-name">${esc(j.file.name)}</div><div class="file-sub">${fmtBytes(j.file.size)}${j.file.type?` / ${esc(j.file.type)}`:""} / 向き：${esc(rotationLabel(rotation))}</div><div style="display:flex;gap:6px;flex-wrap:wrap;margin-top:7px"><button class="rotate-left" type="button" title="左に90度回転" aria-label="${esc(j.file.name)}を左に90度回転" style="padding:5px 8px;font-size:11px" ${runtime.running?"disabled":""}>↶ 左90°</button><button class="rotate-right" type="button" title="右に90度回転" aria-label="${esc(j.file.name)}を右に90度回転" style="padding:5px 8px;font-size:11px" ${runtime.running?"disabled":""}>↷ 右90°</button><button class="rotate-180" type="button" title="180度回転" aria-label="${esc(j.file.name)}を180度回転" style="padding:5px 8px;font-size:11px" ${runtime.running?"disabled":""}>180°</button><button class="rotate-reset" type="button" title="回転を元に戻す" aria-label="${esc(j.file.name)}の回転を元に戻す" style="padding:5px 8px;font-size:11px" ${runtime.running||rotation===0?"disabled":""}>元に戻す</button></div></div></div><div class="job-state ${esc(j.status)}">${esc(stateLabel(j))}</div><button class="remove" type="button" aria-label="削除" ${runtime.running?"disabled":""}>×</button>`;row.querySelector(".rotate-left").onclick=()=>rotateJob(j.id,-90);row.querySelector(".rotate-right").onclick=()=>rotateJob(j.id,90);row.querySelector(".rotate-180").onclick=()=>rotateJob(j.id,180);row.querySelector(".rotate-reset").onclick=()=>resetJobRotation(j.id);row.querySelector(".remove").onclick=()=>removeJob(j.id);els.list.appendChild(row)}}
function percentDelta(oldSize,newSize){if(!oldSize)return null;return(1-newSize/oldSize)*100}
function renderResults(){
 els.outputList.innerHTML="";const resultJobs=jobs.filter(j=>j.output||j.status==="error"),ok=jobs.filter(j=>j.status==="done").length,warn=jobs.filter(j=>j.status==="warn").length,err=jobs.filter(j=>j.status==="error").length;els.resultSummary.textContent=`成功 ${ok} / 警告 ${warn} / 失敗 ${err}`;els.retryFailedBtn.classList.toggle("hidden",!err||runtime.running);els.retryFailedBtn.disabled=runtime.running;
 for(const j of resultJobs){if(j.output){const o=j.output,c=document.createElement("div");c.className="output-card";const d=percentDelta(j.file.size,o.blob.size),delta=d===null?"":d>=0?`${d.toFixed(1)}%削減`:`${Math.abs(d).toFixed(1)}%増加`,deltaClass=d!==null&&d>=0?"good":"bad";const warn=o.sizeMet===false?'<div class="file-sub output-warning">指定容量以下にできませんでした</div>':"";const meta=[o.formatLabel,o.qualityUsed!=null&&o.formatLabel!=="PNG"?`品質 ${Math.round(o.qualityUsed*100)}%`:"",o.rotation?`回転 ${rotationLabel(o.rotation)}`:"",o.metadataRemoved?"メタデータ削除":"メタデータ保持設定"].filter(Boolean).join(" / ");c.innerHTML=`<img alt="preview"><div class="output-info"><div class="file-name">${esc(o.name)}</div><div class="metric-grid"><div class="metric"><b>解像度</b>${o.originalWidth}×${o.originalHeight} → ${o.width}×${o.height}${o.originalWidth===o.width&&o.originalHeight===o.height?"（変更なし）":""}</div><div class="metric"><b>ファイルサイズ</b>${fmtBytes(j.file.size)} → ${fmtBytes(o.blob.size)}${delta?` <span class="delta ${deltaClass}">/ ${delta}</span>`:""}</div></div><div class="file-sub">${esc(meta)}</div>${warn}</div><div class="output-actions"><button class="copy-btn" type="button" ${capabilities.clipboard?"":"disabled"}>${j.copyState==="copied"?"✓ コピー済み":"コピー"}</button><a class="download" download="${esc(o.name)}">保存</a></div>`;c.querySelector("img").src=o.previewUrl||o.url;c.querySelector("a").href=o.url;c.querySelector(".copy-btn").onclick=()=>copyJobOutput(j);els.outputList.appendChild(c)}else if(j.status==="error"){const c=document.createElement("div");c.className="output-card error-card";c.innerHTML=`<div class="error-title">${esc(j.file.name)}：変換に失敗しました</div><div class="file-sub">${esc(j.error?.message||"不明なエラー")}</div><div class="error-code">${esc(j.error?.code||"E_UNKNOWN")}</div><div class="output-actions" style="padding:10px 0 0"><button class="retry-one" type="button" ${runtime.running?"disabled":""}>再試行</button></div>`;c.querySelector(".retry-one").onclick=()=>retryJobs([j]);els.outputList.appendChild(c)}}
 updateMainButtons();updateNamingPreviews();
}
function renderAll(){renderFiles();renderResults();updateMainButtons();updateNamingPreviews()}
function updateMainButtons(){const hasJobs=jobs.length>0,hasOutputs=jobs.some(j=>j.output);els.convertBtn.disabled=runtime.running||!hasJobs;els.clearBtn.disabled=runtime.running||!hasJobs;els.downloadAllBtn.disabled=runtime.running||!hasOutputs;els.zipBtn.disabled=runtime.running||runtime.zipping||!hasOutputs}

function setEngineState(ready,detail=""){engineReady=ready;capabilities.heic=ready;els.engineTitle.textContent=ready?"変換エンジン：準備完了":"変換エンジン：初期化失敗";els.engineSub.textContent=detail||(ready?"HEIC / HEIF変換エンジンをHTML内から初期化しました。":"HEIC / HEIF以外の画像は利用できます。");els.modeBadge.textContent=ready?"完全オフライン版 v11":"完全オフライン版 v11 / HEIC初期化失敗";renderCapabilities()}
function evaluateLibrarySource(source){const factory=new Function(source+'\n;return (typeof libheif!=="undefined" ? libheif : (typeof globalThis.libheif!=="undefined" ? globalThis.libheif : undefined));');return factory()}
async function normalizeLibheif(candidate){let value=candidate;if(typeof value==="function")value=value();if(value&&typeof value.then==="function")value=await value;if(value&&value.ready&&typeof value.ready.then==="function"){const r=await value.ready;if(r&&typeof r.HeifDecoder==="function")value=r}if(!(value&&typeof value.HeifDecoder==="function"))throw new AppError("E_HEIC_ENGINE","libheif の HeifDecoder を取得できませんでした。");return value}
async function initializeEngine(){try{heifModule=await normalizeLibheif(globalThis.libheif);setEngineState(true,"libheif-js 1.19.8 WASM bundle を初期化しました。変換処理はブラウザ内で実行されます。");setStatus("準備完了。画像を追加してください。","success")}catch(e){const er=normalizeError(e,"E_HEIC_ENGINE","HEIC変換エンジンを初期化できませんでした。");console.error(er);setEngineState(false,er.message);setStatus("HEIC / HEIF以外の画像は引き続き変換できます。","warn")}}

async function loadImageBitmapFromFile(file){if(typeof createImageBitmap==="function"){try{return await createImageBitmap(file)}catch(e){console.warn("createImageBitmap fallback",e)}}const url=URL.createObjectURL(file);try{return await new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>reject(new AppError("E_IMAGE_DECODE","この画像形式をブラウザが読み込めませんでした。"));img.src=url})}finally{URL.revokeObjectURL(url)}}
async function loadImageBitmapFromBlob(blob){if(typeof createImageBitmap==="function")return createImageBitmap(blob);const url=URL.createObjectURL(blob);return new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>{URL.revokeObjectURL(url);resolve(img)};img.onerror=()=>{URL.revokeObjectURL(url);reject(new AppError("E_IMAGE_DECODE","画像の再読み込みに失敗しました。"))};img.src=url})}
async function decodeToCanvas(file){
 if(isHeicFile(file)){
  if(!(heifModule&&typeof heifModule.HeifDecoder==="function"))throw new AppError("E_HEIC_ENGINE","HEIC / HEIF変換エンジンが利用できません。");let bytes;try{bytes=new Uint8Array(await file.arrayBuffer())}catch(e){throw new AppError("E_FILE_READ","ファイルを読み込めませんでした。",e)}const decoder=new heifModule.HeifDecoder();let images=null;
  try{images=decoder.decode(bytes);if(!images||!images.length)throw new AppError("E_HEIC_DECODE","HEIF画像が見つかりません。");const image=images[0],width=image.get_width(),height=image.get_height();if(!width||!height)throw new AppError("E_HEIC_DECODE","画像サイズを取得できません。");const canvas=document.createElement("canvas");canvas.width=width;canvas.height=height;const ctx=canvas.getContext("2d",{alpha:true});if(!ctx)throw new AppError("E_CANVAS","Canvasを初期化できません。");const imageData=ctx.createImageData(width,height);await new Promise((resolve,reject)=>image.display(imageData,d=>d?resolve():reject(new AppError("E_HEIC_DECODE","HEIFデコードに失敗しました。"))));ctx.putImageData(imageData,0,0);return{canvas,width,height}}
  finally{if(images)for(const img of images){try{if(img&&typeof img.free==="function")img.free()}catch(_){}}try{if(decoder&&decoder.decoder&&typeof decoder.decoder.delete==="function")decoder.decoder.delete()}catch(_){}}
 }
 let decoded;try{decoded=await loadImageBitmapFromFile(file)}catch(e){throw normalizeError(e,"E_IMAGE_DECODE","画像を読み込めませんでした。")};try{const width=decoded.width||decoded.naturalWidth,height=decoded.height||decoded.naturalHeight;if(!width||!height)throw new AppError("E_IMAGE_DECODE","画像サイズを取得できませんでした。");const canvas=document.createElement("canvas");canvas.width=width;canvas.height=height;const ctx=canvas.getContext("2d",{alpha:true});if(!ctx)throw new AppError("E_CANVAS","Canvasを初期化できません。");ctx.drawImage(decoded,0,0);return{canvas,width,height}}finally{tr